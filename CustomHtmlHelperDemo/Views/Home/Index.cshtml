@model IEnumerable<CustomHtmlHelperDemo.Models.Employee>
@using CustomHtmlHelperDemo.Models;

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@foreach (var item in Model)
{
    @Html.Partial("_Employee", item);
}